[pytest]
pythonpath = . ../src
markers =
    test_config: marks tests for test (deselect with '-m "not_test"')
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
addopts = --strict-markers